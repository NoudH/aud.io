import { FormValidationErrorMatcher } from './form-validation-error-matcher';

describe('FormValidationErrorMatcher', () => {
  it('should create an instance', () => {
    expect(new FormValidationErrorMatcher()).toBeTruthy();
  });
});
